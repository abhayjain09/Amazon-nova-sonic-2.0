# Environment Configuration for Nova Sonic on EC2
# EC2 IAM role provides AWS credentials automatically - no keys needed

# AWS Configuration
AWS_DEFAULT_REGION=us-east-1

# WebSocket Server Configuration
HOST=0.0.0.0
WS_PORT=8084

# Optional: Health Check Port (for load balancer)
# HEALTH_PORT=8082

# Optional: Knowledge Base Configuration
# KB_REGION=us-east-1
# KB_ID=your-knowledge-base-id

# Optional: Bedrock Agents Configuration
# BOOKING_LAMBDA_ARN=your-lambda-arn

# Optional: AWS Profile (not needed with IAM role)
# AWS_PROFILE=your-profile-name

# React Application Configuration
PORT=3025
